# Known Issues

- Live PostgreSQL connectivity and database name are not yet verified. Default to `macro` until proven otherwise.
- Prior completed tasks from deleted MacroForge are historical only and must not be treated as current live completion.
- WDI/schema work must be recreated cleanly from decisions and tests.
- Git remote/push policy remains deferred; push requires human approval.
- Low priority: `state/project_state.md` and `state/architecture.md` are increasingly mixing current operational state with accumulated architectural history. After additional capability transitions, consider separating concise current-state recovery files from longer architectural history/evidence summaries. Do not refactor this now.
